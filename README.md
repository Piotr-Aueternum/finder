# Finder project
Searching data from API with React

# Start
  Preferred environment `node.js@7.x` and `npm@4.x`
  ```
  npm install
  ```

# Development
  ```
  npm run dev
  ```

# Production
  ```
  npm run production
  ```